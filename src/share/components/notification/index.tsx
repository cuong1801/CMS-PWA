// const notification = SNotification().load()

export function errorNotification(description: string): void {
    // notification.then((value: Unknown) => {
    //     value['default']['error']({ message: 'Error', description })
    // })
}

export function successNotification(description: string): void {
    // notification.then((value: Unknown) => {
    //     value['default']['success']({ message: 'Success', description })
    // })
}
